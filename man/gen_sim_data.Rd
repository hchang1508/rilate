% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen_sim_data.R
\name{gen_sim_data}
\alias{gen_sim_data}
\title{Simulate a randomized experiment with noncompliance in one call}
\usage{
gen_sim_data(N, N1, fractions, taus, mode = "constant")
}
\arguments{
\item{N}{Total number of units.}

\item{N1}{Number of units assigned to treatment (\code{Z = 1}); the remaining
\code{N - N1} units are controls.}

\item{fractions}{Numeric vector or list \code{c(fa, fc, fn)} of population
fractions for always-reporters, compliers, and never-reporters. Must sum to
\enumerate{
\item Stratum counts are \code{round(fa * N)} and \code{round(fc * N)}, with the
never-reporter count taken as the remainder so the three counts sum to \code{N}
exactly regardless of rounding.
}}

\item{taus}{Numeric vector or list \code{c(tau_a, tau_c, tau_n)} of
principal-stratum treatment effects for always-reporters, compliers, and
never-reporters.}

\item{mode}{Either \code{"constant"} (default) to give every unit in a stratum
exactly its \code{tau}, or \code{"heterogeneous"} to draw unit-level effects
\code{tau * rnorm(n, 1, 0.1)} with the stratum mean calibrated to \code{tau}.}
}
\value{
A list with two elements: \code{true_table}, the full potential-outcome
table from \code{gen_data()}; and \code{observed}, one realized sample from
\code{gen_data_onesim()} with columns \code{Y_observed}, \code{D_observed}, \code{assignment}.
}
\description{
Builds a fixed potential-outcome ("true") table for a population split into
always-reporters, compliers, and never-reporters, then draws one completely
randomized experiment from it. By default treatment effects are constant
within each principal stratum (principal-strata-constant-effect); set
\code{mode = "heterogeneous"} for unit-level heterogeneity around each stratum's
target.
}
