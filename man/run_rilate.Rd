% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_rilate.R
\name{run_rilate}
\alias{run_rilate}
\title{Run the Anderson-Rubin confidence-set procedure (wrapper)}
\usage{
run_rilate(
  data,
  y = "Y_observed",
  d = "D_observed",
  z = "assignment",
  x = NULL,
  algorithm = c("algo2", "algo1"),
  n_rand = 1000,
  with_covariate = TRUE,
  alpha = 0.95,
  tol = 1e-08,
  cond_threshold = 1e+10,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{Data frame containing the outcome, treatment-received, assignment,
and (optionally) covariate columns.}

\item{y, d, z}{Names of the outcome, treatment-received, and assignment columns
in \code{data}. Default to \code{"Y_observed"}, \code{"D_observed"}, \code{"assignment"}.}

\item{x}{Optional character vector naming the covariate columns to use. If
\code{NULL} (default), no covariates are used and the covariate-adjusted version
is skipped. If supplied, only the named columns are used as covariates (they
must exist in \code{data} and must not name any of \code{y}/\code{d}/\code{z}); any other
columns of \code{data} are ignored.}

\item{algorithm}{Which algorithm to use: \code{"algo2"} (default, fast) or
\code{"algo1"}.}

\item{n_rand}{Number of randomizations (permuted assignments) to draw.
Default \code{1000}.}

\item{with_covariate}{If \code{TRUE} (default) and covariates are present, prepare
both the with- and without-covariates analyses. If \code{FALSE}, prepare only the
without-covariates analysis.}

\item{alpha}{Confidence level (default \code{0.95}).}

\item{tol}{Numerical tolerance passed to the algorithm (default \code{1e-8}).}

\item{cond_threshold}{Condition-number cutoff above which a treatment group's
covariate design \verb{[1, X]} is treated as ill-conditioned (default \code{1e10}).}

\item{seed}{Optional integer seed for reproducible \code{zsim} generation.}

\item{verbose}{If \code{TRUE} (default), print the setup report and the algorithm's
progress/results to the console; if \code{FALSE}, run silently (no output at
all). The returned value is identical either way.}
}
\value{
A list with the prepared inputs and metadata: \code{algorithm}, \code{alpha},
\code{tol}, \code{N}, \code{N1}, \code{N0}, \code{runs} (which versions were prepared), \code{covariates},
\code{zsim}, \code{inputs} (the \code{without_covariates} / \code{with_covariates} data tables),
\code{guard} (per-group rank and condition numbers), and \code{results}. Each element
of \code{results} (\code{without_covariates} / \code{with_covariates}) is the algorithm
output -- a list with \code{confidence_set}
(a list of \verb{[lower, upper]} intervals) and \code{p_value} (the randomization
p-value for the null \code{beta = 0}).
}
\description{
Convenience wrapper that prepares data for and dispatches to the
Anderson-Rubin permutation algorithms (\code{AR_algo1_custom} /
\code{AR_algo2_custom}). It standardizes column names, demeans covariates, checks
the per-treatment-group covariate design for ill-conditioning, and builds the
matrix of permuted assignments.
}
\details{
When covariates are supplied and \code{with_covariate = TRUE} (the default), both a
without-covariates and a with-covariates analysis are prepared. Set
\code{with_covariate = FALSE}, or supply no covariates, to prepare only the
without-covariates analysis.

The selected algorithm is run on each prepared version and its confidence set
and randomization p-value are returned.
}
