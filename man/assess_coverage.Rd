% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess_coverage.R
\name{assess_coverage}
\alias{assess_coverage}
\title{Assess test size and confidence-set coverage by simulation}
\usage{
assess_coverage(
  N,
  N1,
  fractions,
  taus,
  mode = "constant",
  nsim = 100,
  algorithm = c("algo2", "algo1"),
  n_rand = 1000,
  alpha = 0.95,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{N}{Total number of units.}

\item{N1}{Number of units assigned to treatment (\code{Z = 1}); the remaining
\code{N - N1} units are controls.}

\item{fractions}{Numeric vector or list \code{c(fa, fc, fn)} of population
fractions for always-reporters, compliers, and never-reporters. Must sum to
\enumerate{
\item Counts are \code{round(fa * N)} and \code{round(fc * N)}, with the never-reporter
count taken as the remainder so the three counts sum to \code{N} exactly.
}}

\item{taus}{Numeric vector or list \code{c(tau_a, tau_c, tau_n)} of
principal-stratum treatment effects for always-reporters, compliers, and
never-reporters. The complier effect \code{tau_c} is the true LATE whose coverage
is assessed.}

\item{mode}{Either \code{"constant"} (default) to give every unit in a stratum
exactly its \code{tau}, or \code{"heterogeneous"} for unit-level effects calibrated to
each stratum's mean.}

\item{nsim}{Number of simulated experiments to draw from the fixed true table.}

\item{algorithm}{Which algorithm \code{\link[=run_rilate]{run_rilate()}} should use: \code{"algo2"}
(default, fast) or \code{"algo1"}.}

\item{n_rand}{Number of randomizations (permuted assignments) per call to
\code{\link[=run_rilate]{run_rilate()}}. Default \code{1000}.}

\item{alpha}{Confidence level for the confidence set; the test significance
level is \code{1 - alpha}. Default \code{0.95}.}

\item{seed}{Optional integer seed for reproducibility. When supplied, both the
true table and all simulation draws are reproducible.}

\item{verbose}{If \code{TRUE}, print a one-line progress update every \verb{10\%} of the
run. The (very verbose) internal output of \code{\link[=run_rilate]{run_rilate()}} is always
suppressed. Default \code{TRUE}.}
}
\value{
A list with elements: \code{size} (empirical rejection rate of H0: LATE = 0
at level \code{1 - alpha}), \code{coverage} (empirical coverage of the confidence set),
\code{true_late}, \code{nsim}, \code{alpha}, \code{sig_level}, \code{counts} (the \code{N_at}/\code{N_c}/\code{N_nt}
stratum sizes), and the per-simulation vectors \code{p_values}, \code{covered}, and
\code{rejected}.
}
\description{
Builds a single fixed "true" potential-outcome table for a population split
into always-reporters, compliers, and never-reporters, then repeats the
following \code{nsim} times: draw one completely randomized experiment from that
table with \code{\link[=gen_data_onesim]{gen_data_onesim()}}, run \code{\link[=run_rilate]{run_rilate()}} on it, and record the
randomization p-value (for the null LATE = 0) and whether the true LATE lies
in the confidence set. The true LATE is the complier treatment effect,
\code{taus[2]}.
}
\details{
Aggregating across the \code{nsim} draws yields the empirical \strong{size} of the test
(the fraction of draws that reject at level \code{1 - alpha}) and the empirical
\strong{coverage} of the confidence set (the fraction of draws whose set contains
the true LATE). When the true LATE is non-zero the reported "size" is really
the rejection rate / power of the LATE = 0 test.
}
